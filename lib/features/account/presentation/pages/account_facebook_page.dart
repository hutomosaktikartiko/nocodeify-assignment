import 'package:flutter/material.dart';

class AccountFacebookPage extends StatelessWidget {
  const AccountFacebookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Facebook Account')));
  }
}
