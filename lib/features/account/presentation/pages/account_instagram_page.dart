import 'package:flutter/material.dart';

class AccountInstagramPage extends StatelessWidget {
  const AccountInstagramPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Instagram Account')));
  }
}
